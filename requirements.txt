# --- Arch Linux setup ---
# sudo pacman -S tk python-pip
# cd /home/antonio/Desktop/AdvancedScraping
# python3 -m pip install --user -r requirements.txt
# python3 dashboard.py
#
# tk = tkinter (required for customtkinter GUI)
# windnd is Windows-only; drag-and-drop on Linux uses file picker instead

pandas
openpyxl
xlrd>=2.0.1
customtkinter
windnd; sys_platform == "win32"
